{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "docker/Dockerfile.links"
  },
  "deploy": {
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 600,
    "restartPolicyType": "ON_FAILURE"
  }
}
