{
  "name": "peal-actions",
  "version": "0.0.1",
  "private": true,
  "description": "Peal Private Actions: agent intents that stay encrypted until batch inclusion and ordering are committed, then settle with a verifiable receipt.",
  "license": "Apache-2.0",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bte-sdk": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
